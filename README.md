# IMDb Score Finder (Chrome Extension)

A lightweight Chrome extension built with **React** that shows the **IMDb rating** for the movie or TV show you’re viewing by fetching data from the **OMDb API**.

---

## Features

- 🔎 Detects the current title (movie / series) from the page you’re on
- ⭐ Fetches and displays the **IMDb rating** (and optional extra metadata) via **OMDb API**
- ⚡ Fast, minimal UI built with React
- 🧩 Works as a Chrome Extension (Manifest V3)

---

## Demo

> Add screenshots / GIFs here (popup view + example on a page)

---

## Tech Stack

- **React**
- **Chrome Extensions (Manifest V3)**
- **OMDb API** for title search and IMDb rating lookup

---

## Getting Started

### 1) Clone and install

```bash
git clone https://github.com/kxngollan/imdb
cd imdb
npm install
```
